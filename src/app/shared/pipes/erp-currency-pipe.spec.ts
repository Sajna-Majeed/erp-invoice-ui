import { ErpCurrencyPipe } from './erp-currency-pipe';

describe('ErpCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ErpCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
