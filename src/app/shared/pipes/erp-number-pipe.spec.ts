import { ErpNumberPipe } from './erp-number-pipe';

describe('ErpNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ErpNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
